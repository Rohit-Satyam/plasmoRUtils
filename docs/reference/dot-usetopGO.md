# plasmoRUtils

Hidden function called by `easytopGO` to run topGO.

## Usage

``` r
.usetopGO(
  stats = stats,
  category = category,
  geneID = geneID,
  gene_2_GO = gene_2_GO,
  algo = algo,
  fdr = fdr,
  correction = correction
)
```
